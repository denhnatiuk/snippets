function multipleKaratsuba(num1, num2) {
  // nums as strings
  numlen: function numlen(num){
    return num.toString().length
  }
  strslice: function sliceasstr(num){
    num.toString().slice(-2,-1)
  }
  // base
  if (numlen(num1) < 2 || numlen(num2) < 2) {
    return num1*num2
  }
  // recursive
  else {

  }

  // pure numerical
  if (num < 1000){
    let a =
    let b = 
  }
}
